<%= form_with(model: beer, local: true) do |form| %>
  <% if beer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(beer.errors.count, "error") %> prohibited this beer from being saved:</h2>

      <ul>
      <% beer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :beer_name %>
  </div>

  <div class="field">
    <%= form.label :beer_type %>
    <%= form.text_field :beer_type, id: :beer_type %>
  </div>

  <div class="field">
    <%= form.label :country %>
    <%= form.text_field :country, id: :beer_country %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :beer_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
