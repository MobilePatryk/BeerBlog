<p id="notice"><%= notice %></p>
	<ul class="listing">
		<div class="row">
			<div class="well col-md-4 col-md-offset-4">
				<li><%= link_to avatar_for(@beer.user), user_path(@beer.user) %></li>
				<li class="beer-title">
					<%=	link_to @beer.user.username, user_path(@beer.user) %>
				</li>
				<li><small><%= pluralize(@beer.user.beers.count, "beers") if @beer.user.beers %></small>
				</li>
			</div>
		</div>
	</ul>
<p>
  <strong>Name:</strong>
  <%= @beer.name %>
</p>

<p>
  <strong>Type:</strong>
  <%= @beer.beer_type %>
</p>

<p>
  <strong>Country:</strong>
  <%= @beer.country %>
</p>

<p>
  <strong>Price:</strong>
  <%= @beer.price %>
</p>

<%= link_to 'Edit', edit_beer_path(@beer) %> |
<%= link_to 'Back', beers_path %>
